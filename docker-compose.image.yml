# 换机部署用：仅需先 docker load，再本文件启动（无需源码目录、无需 .env）
services:
  gemini-h5:
    image: ${DOCKER_IMAGE:-gemini-h5:latest}
    container_name: gemini-h5
    restart: unless-stopped
    ports:
      - "10000:10000"
    volumes:
      - gemini-h5-data:/opt/gemini-h5/backend/data

volumes:
  gemini-h5-data:
